' S-038: FOR EACH döngüye girerken listenin ANLIK GÖRÜNTÜSÜNÜ dolaşır; gövdede eklenen üye bu turda görünmez.
DIM l AS LIST
DIM v AS I32
LISTADD(l, 1)
LISTADD(l, 2)
FOR EACH v IN l
    PRINT v
    IF v = 1 THEN
        LISTADD(l, 3)
    END IF
NEXT
PRINT LISTLEN(l)
