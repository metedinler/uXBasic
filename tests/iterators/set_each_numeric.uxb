' S-038: SET üyeleri string saklanır; sayısal döngü değişkenine AÇIK dönüşümle bağlanır (native x64 eskiden işaretçi yazıyordu).
DIM s AS SET
DIM x AS I32
SETADD(s, 7)
SETADD(s, 9)
FOR EACH x IN s
    PRINT x + 1
NEXT
