# uXBasic library tests

Canonical library smoke and ABI tests live under this tree. Ecosystem-wide,
cross-platform static checks are in `tests/ecosystem`; Windows runtime suites
may add module-specific folders without placing tests under `src`.
