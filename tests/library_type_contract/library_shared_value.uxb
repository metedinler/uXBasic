LIBTYPE "shared_text.library_type.json"
MAIN
    DIM a AS TEXT_REF
    DIM b AS TEXT_REF
    DIM c AS TEXT_REF
    a = TEXT_REF.CONSTRUCT("hello")
    b = a
    c = TEXT_REF.CLONE(a)
    a = TEXT_REF.NORMALIZE(a)
    PRINT b
    PRINT c
    CALL TEXT_REF.DROP(a)
    PRINT b
END MAIN
