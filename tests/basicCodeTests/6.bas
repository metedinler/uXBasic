DIM sira AS I32
DIM sure AS I32

sira = 1
DO WHILE sira <= 3
    sure = (sira * 10) + 5
    PRINT sure
    sira = sira + 1
LOOP

DIM hata AS I32
hata = 0
DO
    PRINT hata
    hata = hata - 1
LOOP UNTIL hata = -1