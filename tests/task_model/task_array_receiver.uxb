' S-030: TASK dizisi elemani dogrudan alici olabilir: g(i).JOIN(), g(i).STATUS.
THREAD Worker(x AS I32)
    RETURN x * 3
END THREAD

DIM g(4) AS TASK
DIM i AS I32
FOR i = 1 TO 4
    g(i) = SPAWN Worker(i)
NEXT
FOR i = 1 TO 4
    PRINT g(i).JOIN()
NEXT
PRINT g(2).STATUS
PRINT (g(4)).RESULT
