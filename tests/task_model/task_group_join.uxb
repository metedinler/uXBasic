' S-030: TASKGROUP.ADD/JOIN; uyelerin sonuclari tek tek RESULT ile okunur.
THREAD Worker(x AS I32)
    RETURN x * 2
END THREAD

DIM g AS TASKGROUP
DIM t1 AS TASK = SPAWN Worker(1)
DIM t2 AS TASK = SPAWN Worker(2)
g.ADD(t1)
g.ADD(t2)
g.ADD(SPAWN Worker(3))
g.JOIN()
PRINT t1.RESULT
PRINT t2.RESULT
