' S-030: PARALEL gorevleri; sonuclar BASLATMA sirasina gore okunur.
PARALEL Square(x AS I32)
    RETURN x * x
END PARALEL

DIM a AS TASK = SPAWN Square(3)
DIM b AS TASK = SPAWN Square(4)
DIM c AS TASK = SPAWN Square(5)
PRINT a.JOIN()
PRINT b.JOIN()
PRINT c.JOIN()
