' S-030: hatali gorevin RESULT okunusu hatayi cagirana aktarir.
THREAD Boom(x AS I32)
    THROW "patladi"
    RETURN x
END THREAD

DIM b AS TASK = SPAWN Boom(1)
PRINT "once"
PRINT b.RESULT
PRINT "sonra"
