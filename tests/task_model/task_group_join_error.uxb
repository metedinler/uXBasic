' S-030: TASKGROUP.JOIN ilk hatali uyenin hatasini (ekleme sirasina gore) aktarir.
THREAD Worker(x AS I32)
    RETURN x
END THREAD
THREAD Boom(x AS I32)
    THROW "grup hatasi"
    RETURN x
END THREAD

DIM g AS TASKGROUP
g.ADD(SPAWN Worker(1))
g.ADD(SPAWN Boom(2))
PRINT "once"
g.JOIN()
PRINT "sonra"
