' S-030 K6: WAIT deger dondurmez; bitmis gorevde CANCEL zararsizdir, tekrar CANCEL de zararsizdir.
THREAD Worker(x AS I32)
    RETURN x * 10
END THREAD

DIM t AS TASK = SPAWN Worker(4)
t.WAIT()
PRINT t.STATUS
t.CANCEL()
t.CANCEL()
PRINT t.STATUS
PRINT t.RESULT
