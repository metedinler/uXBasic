' S-030: gorev govdesindeki hata gorev nesnesinde tutulur; ERRORTEXT fırlatmadan okur,
' program RESULT okunmadikca devam eder.
THREAD Boom(x AS I32)
    THROW "patladi"
    RETURN x
END THREAD

DIM b AS TASK = SPAWN Boom(1)
PRINT b.STATUS
IF b.ERRORTEXT <> "" THEN PRINT "hata var"
PRINT "devam"
