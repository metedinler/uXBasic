' S-030: TASK donduren FUNCTION'un sonucu dogrudan alici olabilir.
THREAD Worker(x AS I32)
    RETURN x + 100
END THREAD

FUNCTION Start(v AS I32) AS TASK
    DIM t AS TASK = SPAWN Worker(v)
    RETURN t
END FUNCTION

PRINT Start(5).JOIN()
PRINT Start(6).STATUS
DIM k AS TASK
k = Start(7)
PRINT k.RESULT
