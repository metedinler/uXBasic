' S-030 Faz 1: THREAD hedefine SPAWN, sonuc JOIN/RESULT ile alinir.
THREAD Worker(x AS I32)
    RETURN x + 1
END THREAD

DIM t AS TASK = SPAWN Worker(41)
PRINT t.STATUS
PRINT t.RESULT
PRINT t.JOIN()
PRINT "[" + t.ERRORTEXT + "]"
